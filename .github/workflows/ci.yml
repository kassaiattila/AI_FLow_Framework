name: AIFlow CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python-lint-test:
    name: Python Lint + Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv venv && uv pip install -e ".[dev]"

      - name: Lint (ruff)
        run: |
          .venv/bin/ruff check src/ tests/
          .venv/bin/ruff format --check src/ tests/

      - name: Unit tests
        run: .venv/bin/pytest tests/unit/ -q --tb=short --junitxml=test-results.xml --cov=aiflow --cov-report=xml
        env:
          AIFLOW_DATABASE_URL: "postgresql://localhost/test"

      - name: Coverage gate (80% minimum)
        run: |
          .venv/bin/python -c "
          import xml.etree.ElementTree as ET
          tree = ET.parse('coverage.xml')
          rate = float(tree.getroot().attrib.get('line-rate', 0))
          pct = round(rate * 100, 1)
          print(f'Coverage: {pct}%')
          assert pct >= 80, f'Coverage {pct}% is below 80% gate'
          "

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: python-test-results
          path: |
            test-results.xml
            coverage.xml

  admin-build:
    name: Admin Dashboard Build (Vite + React)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: aiflow-admin
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: aiflow-admin/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
