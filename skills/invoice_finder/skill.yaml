name: invoice_finder
display_name: "Invoice Finder"
version: "3.0.0"
description: "Mailbox scan → document acquisition → classify → extract → report → notify"
skill_type: ai

capabilities:
  - mailbox_scanning
  - document_acquisition
  - invoice_classification
  - field_extraction
  - payment_status_check
  - file_organization
  - report_generation
  - notification

required_models:
  - name: openai/gpt-4o
    type: generation
    usage: "Invoice field extraction and report generation"
  - name: openai/gpt-4o-mini
    type: generation
    usage: "Email scoring, classification, and payment status"

workflows:
  - invoice-finder

estimated_cost_per_run: 0.15
tags: [invoice, finder, email, scan, extraction, hungarian, bestix]
depends_on: []
