# AIFlow Sprint E — Session 60 Prompt (E1.4: Week 1 Day 5 — E2E round 1 + Week 1 gate)

> **Datum:** 2026-04-26 (Week 1 Day 5)
> **Branch:** `feature/v1.4.1-phase-1b-sources`
> **HEAD:** `5f58312` (feat(sources): E1.3 — FileSourceAdapter (push mode) + 23 unit tests)
> **Port:** API 8102 | Frontend 5174
> **Elozo session:** S59 / E1.3 — FileSourceAdapter (push transport, requires_ack=False) producing IntakePackage with exactly 1 IntakeFile via enqueue() + FIFO drain via fetch_next(); shared sanitize_filename helper extracted to sources/_fs.py; email_adapter refactored onto the helper; 23 file_adapter unit tests (bytes/path xor, MIME detect + DI override, size guard, sanitization, FIFO ack/reject, USER_NOTE description) + 3 registry integration tests; 1737 → 1763 unit PASS, sources 63 → 89 PASS, 199 Phase 1a E2E no regression, ruff clean.
> **Terv:** `01_PLAN/session_S55_v1_4_1_phase_1b_sources_kickoff.md` (Week 1 Day 5 — E2E round 1 + Week 1 exit gate)
> **Session tipus:** TESTING (real-pipeline E2E + Week 1 gate evaluation)

---

## KONTEXTUS

### Honnan jottunk
Week 1 Days 1–4 lezartak az Email + File oldalt:
- `EmailSourceAdapter` (`ImapBackend` + `OutlookComBackend`) — pull transport, `requires_ack=True`
- `FileSourceAdapter` — push transport, `requires_ack=False`, `enqueue()` + FIFO drain
- Shared `sources/_fs.sanitize_filename` helper; both adapters use it
- 89 sources unit PASS; registry `EMAIL` + `FILE_UPLOAD` coexist, duplicate guarded

A ket E2E placeholder (`test_email_source.py`, `test_file_source.py`) es ett skipped — Day 5 feladata un-skipelni oket valos pipeline-on.

### Hova tartunk — Week 1 Day 5 (E1.4)
Az adapterek mar keszek; hianyzik a valos E2E hitel:
- `test_email_source.py` — IMAP backend (FakeImapBackend lehet, de a pipeline legyen real: DB + storage + acknowledge)
- `test_file_source.py` — REST-szeru enqueue → IntakePackage → routing → acknowledge
- **Week 1 exit gate:** 2 adapter kesz, >=40 adapter-kozponti unit test, >=4 un-skipped E2E test, ruff clean, regression PASS

A Day 6 (E2.1 FolderSourceAdapter) utana indul — scope-brakes NEM engedettek a Week 1 gate-ig.

### Jelenlegi allapot (E1.3 utan)
```
27 service | 175 endpoint | 49 DB tabla | 34 Alembic migration
1763 unit (1737 pre + 26 E1.3) | 384 E2E collect (169 pre + 199 Phase 1a + 16 Phase 1b)
7 skill | 23 UI oldal | 2 source adapter (Email + 2 backends, File)
```

---

## ELOFELTETELEK

```bash
git branch --show-current        # feature/v1.4.1-phase-1b-sources
git log --oneline -3             # HEAD: 5f58312 (E1.3)

.venv/Scripts/python.exe -m pytest tests/unit/sources/ -v        # 89 PASS
.venv/Scripts/python.exe -m pytest tests/unit/ -x -q             # 1763 PASS
.venv/Scripts/python.exe -m ruff check src/ tests/               # 0 error
PYTHONPATH=src .venv/Scripts/python.exe -m alembic current       # 034 (head)

# E2E placeholders jelenleg skipped-ek:
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_1_phase_1b/test_email_source.py -v
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_1_phase_1b/test_file_source.py -v
```

---

## FELADATOK

### LEPES 1: `test_file_source.py` un-skip (>=2 real E2E)

```
Cel:    File upload → IntakePackage → PolicyEngine routolas → acknowledge
Fajl:   tests/e2e/v1_4_1_phase_1b/test_file_source.py (mar letezik, skipped)
Forras: session_S55_*.md Day 5, 101_*.md R1

Tesztek:
  - test_file_source_happy_path():
      * FileSourceAdapter(storage_root=tmp, tenant_id="t_e2e_file")
      * enqueue(raw_bytes=sample_pdf, filename="invoice.pdf")
      * fetch_next() visszaadja a package-et
      * Assert: package.source_type == FILE_UPLOAD
      * Assert: 1 file on disk sha256 == expected
      * Assert: acknowledge idempotens az elso utan → SourceAdapterError
  - test_file_source_reject_unsupported_mime():
      * Adapter MIME whitelist-et NEM kezeli (az a PolicyEngine dolga)
      * Helyette: max_package_bytes guard tesztje (oversize → SourceAdapterError,
        NEM kerul be a queue-ba, storage-ba sem ir)
      * VAGY: un-skipeld EZT a tesztet csak akkor, ha a PolicyEngine integracio
        Day 5-re eleri a branch-et — kulonben irj egyetlen pozitiv + egy
        size-guard negativ tesztet.
```

### LEPES 2: `test_email_source.py` un-skip (>=2 real E2E)

```
Cel:    IMAP fetch → IntakePackage → acknowledge → \\Seen flag beallitva
Fajl:   tests/e2e/v1_4_1_phase_1b/test_email_source.py (mar letezik, skipped)
Forras: session_S55_*.md Day 5, 101_*.md N2

Tesztek:
  - test_email_source_happy_path():
      * EmailSourceAdapter + FakeImapBackend(2 multipart message)
      * fetch_next() → IntakePackage (2 IntakeFile + 1 EMAIL_BODY description)
      * acknowledge(pkg.package_id) → backend.seen_uids tartalmaz
      * Storage: attachments spilled to storage_root/{tenant}/{package_id}/
  - test_email_source_reject_oversize_attachment():
      * max_package_bytes=1_024 + 50 KB attachment
      * fetch_next() → None (package skipped)
      * backend.flagged[uid] == "size_exceeded"
      * No package_id leaked into adapter internal map

Megjegyzes: FakeImapBackend elegendo Day 5-re — REAL IMAP (Outlook COM vagy
Docker MailDev) Week 2 vegi hardening feladat (E2.4+).
```

### LEPES 3: Week 1 exit gate validacio

```bash
# Unit coverage ellenorzes
.venv/Scripts/python.exe -m pytest tests/unit/sources/ -q         # 89+ PASS

# Ket E2E fajl tobb mar NEM skipped
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_1_phase_1b/test_email_source.py \
    tests/e2e/v1_4_1_phase_1b/test_file_source.py -v              # >=4 PASS

# Full regression
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_0_phase_1a/ -q  # 199 PASS
.venv/Scripts/python.exe -m pytest tests/unit/ -x -q              # 1763+ PASS

# Ruff + typecheck (csak ha aiflow-admin/ erintett — ebben a session-ben NEM)
.venv/Scripts/python.exe -m ruff check src/ tests/                # 0 error
```

Gate-lista (KOTELEZO minden tetel `[x]` legyen):
- [ ] 2 adapter (Email + File) production-ban
- [ ] >=40 adapter-unit test (jelenleg 89 — OK)
- [ ] >=4 un-skipped E2E test (email: 2 + file: 2 = 4)
- [ ] 199 Phase 1a E2E regresszio-mentes
- [ ] ruff clean
- [ ] Week 1 exit summary a commit uzenetben

### LEPES 4: Commit + session-close

```bash
git add tests/e2e/v1_4_1_phase_1b/test_email_source.py \
        tests/e2e/v1_4_1_phase_1b/test_file_source.py

/session-close E1.4
```

---

## STOP FELTETELEK

ALLJ MEG es kerj iranymutatast, ha:
- A PolicyEngine integracio szukseges a "reject_unsupported_mime" teszthez, de meg nem landolt a branchbe → un-skipeld csak a size-guard negativ tesztet, hagyd a MIME rejection-t Week 2-re
- Real IMAP tesztkornyezet felmerul (Docker MailDev, dovecot) → Day 5 hatarozottan FakeImapBackend, a real IMAP E2E Week 2 vege (E2.4+)
- Week 1 exit gate barmely tetele NEM teljesul → STOP, bontsd E1.4-A (email un-skip) + E1.4-B (file un-skip + gate) sessionre
- Adapter ABC breaking change merul fel a tesztekbol → STOP, architekt review (scope > 1 session)
- FakeImapBackend hianyos a 2-adatas scenario-hoz → bovitsd a `tests/unit/sources/test_email_adapter.py`-bol, NE vezess be uj fake-ot az E2E mappaban

---

## SESSION VEGEN

```
/session-close E1.4
```

Utana S61 / E2.1 = Week 2 Day 6 (FolderSourceAdapter — watchdog + debounce + mid-write guard).

---

*Sprint E session: S60 = E1.4 (Phase 1b Week 1 Day 5 — E2E round 1 un-skip + Week 1 exit gate)*
