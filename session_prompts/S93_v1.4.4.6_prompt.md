# AIFlow Sprint H — Session 93 Prompt (v1.4.4.6: test_alembic_034 + out/ cleanup + CLAUDE.md sync)

> **Datum:** 2026-04-28
> **Branch:** `feature/v1.4.4-consolidation`
> **HEAD:** `e687681` docs(v1.4.4): S92 — ROADMAP.md + 104_ drift fix + v1.4.4 PR draft
> **Port:** API 8102 | Frontend Vite :5173
> **Elozo session:** S92 — ROADMAP.md, `104_*` §7.2/§8.1/§10.3 drift correction, `docs/v1.4.4_pr_description.md` draft. Unit 1898 PASS, integration 42 PASS, ruff exit 0. Commit `e687681`.
> **Terv:** `01_PLAN/ROADMAP.md` (Active sprint table → S93 row) + `01_PLAN/session_S88_v1_4_4_consolidation_kickoff.md` §"Session breakdown — S93".
> **Session tipus:** Sprint close housekeeping (docs + test hygiene + repo cleanup). Code risk: low. Process risk: low.

---

## KONTEXTUS

### Honnan jottunk

S88-S92 befejezte a v1.4.4 konszolidacios sprintet:
- S88: version reconcile (1.3.0 → 1.4.4), port doc fix, ~25 session prompt archive.
- S89: frontend dev-env live, Untitled UI ADR.
- S90: journey E2E triage + admin contract regressions.
- S91: `test_auth` leak fix, e2e asyncio markers, 4-session coverage roadmap (HARD STOP, issue #7 open).
- S92: `ROADMAP.md` (forward queue), `104_*` Phase 1a scope correction (3 contract + 1 state machine actually delivered, 10 contract + 6 state machine deferred to §10.3), v1.4.4 PR draft.

### Hova tartunk

S93 = a v1.4.4 sprint utolso session-je. Harom maradek housekeeping tetel + PR kivagas + `v1.4.4` tag.

- **A** — `tests/e2e/v1_4_1_phase_1b/test_alembic_034.py` head assertion drift véglegesen zárja (S88 head-relative patch volt — most `alembic.script.ScriptDirectory` vagy a migracios head snapshot-jat hasznaljuk, hogy ne rohaggyon minden Alembic head bump-nal).
- **B** — `out/` repo backlog cleanup: ~25 untracked log/json fajl (`api_log*.txt`, `b6_validation_round*.md`, `phase_1b_merge_gate_*.log`, `s89/s90/s91_*.log`, `cli_test.*`, `diagram_eval.json`, `hardening_*.md`, `mypy_*.log`, `spec_eval.json`, `token.*`). Ami referencialt (`out/s91_coverage_plan.md`), az marad. A többi vagy töröl, vagy `.gitignore`.
- **C** — `CLAUDE.md` "Key Numbers" sync: unit 1898 (korabban 1886), integration 42, Alembic head 037, 27 service / 177 endpoint / 49 DB tabla. Ha elter a valos allapottol, frissitsd.

### Jelenlegi allapot

```
27 service | 177 endpoint | 49 DB tabla | 37 Alembic migration (head: 037)
1898 unit PASS | 42 integration PASS | 410 E2E collected | 199 Phase 1a E2E green | 35 Phase 1b E2E green
Coverage: 67.00% (issue #7 OPEN, gate waiver marad)
```

---

## ELOFELTETELEK

```bash
git branch --show-current                             # feature/v1.4.4-consolidation
git log --oneline -3                                  # top: e687681 docs(v1.4.4): S92...
.venv/Scripts/python.exe -m pytest tests/unit/ -x -q --no-cov   # 1898 PASS
.venv/Scripts/python.exe -m ruff check src/ tests/ --quiet      # exit 0
PYTHONPATH=src .venv/Scripts/python.exe -m alembic current      # 037 (head)
```

---

## FELADATOK

### LEPES 1 — `test_alembic_034` head assertion véglegesítése (A)

Fajl: `tests/e2e/v1_4_1_phase_1b/test_alembic_034.py`

- S88 head-relative fix volt; nezd meg a 118 es 137 sorat (vagy az aktualisat).
- Nyisd `alembic.script.ScriptDirectory.from_config()` + `.get_current_head()` mintaval — sose hardcode-olt rev id.
- Az eredeti teszt scope (034 migration specific) maradjon — csak a head assertion valjon dinamikussa.
- Unit gyanant fut? Hagyd e2e-ben (tesztkategoria nem valtozik).

### LEPES 2 — `out/` cleanup (B)

```bash
git status --short | grep '^?? out/'   # listazd
```

Tegyel dontest fajlonkent:
- **Torlendo** (ideiglenes log): `api_log*.txt`, `cli_test.json`, `cli_test.md`, `diagram_eval.json`, `phase_1b_merge_gate_*.log`, `s89_journey_raw.log`, `s89_vite_dev.log`, `s90_journey_rerun.log`, `mypy_after_fixes.log`, `spec_eval.json`, `token.json`, `token.txt`, `hardening_*.md` (ha nincs rajuk terv-referencia), `b6_validation_round*.md` (ha a sprint mar zart).
- **Maradjon** (planra hivatkozott): `out/s91_coverage_plan.md`.
- **.gitignore bovites:** ha `out/` szabaly mar nincs, add be a mintat: `out/*.log`, `out/*.json`, `out/token.*`, `out/api_log*.txt`.

Validald: a cleanup utan `git status --short` tiszta (csak a `.claude/` modifikaciok maradjanak, ha relevans).

### LEPES 3 — `CLAUDE.md` key-numbers sync (C)

Mezok amiket aktualizalj:
- `1886 unit tests` → `1898 unit tests`.
- `42 integration tests` — ha mar ott van, hagyd; ha nem, pot-keld be.
- `37 Alembic migrations` — verifikald.
- `v1.4.3` vezeto cim → `v1.4.4` (ha a tag cut-re keszulunk). Vagy hagyd v1.4.3-nak es S94-ben frissul.
- `Current Plan` szekcio → `01_PLAN/ROADMAP.md` mint uj egyetlen entry point.

### LEPES 4 — `01_PLAN/ROADMAP.md` frissites

- S92 status `IN-PROGRESS` → `DONE`.
- S93 status `QUEUED` → `IN-PROGRESS` (majd a session vegen `DONE`).

### LEPES 5 — Validacio

```bash
.venv/Scripts/python.exe -m ruff check src/ tests/ --quiet     # exit 0
.venv/Scripts/python.exe -m pytest tests/unit/ -x -q --no-cov  # 1898+ PASS
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_1_phase_1b/test_alembic_034.py -q  # PASS
.venv/Scripts/python.exe -m pytest tests/integration/ -x -q --no-cov  # 42+ PASS
```

### LEPES 6 — Commit

```bash
git add tests/e2e/v1_4_1_phase_1b/test_alembic_034.py \
        CLAUDE.md 01_PLAN/ROADMAP.md .gitignore
# + torolt out/ fajlok (git rm / git clean per file)

git commit -m "$(cat <<'EOF'
chore(v1.4.4): S93 — test_alembic_034 head-relative + out/ cleanup + CLAUDE.md sync

- Convert tests/e2e/v1_4_1_phase_1b/test_alembic_034.py head assertion to
  alembic.script.ScriptDirectory dynamic lookup (from 035 hardcode). Test no
  longer breaks on Alembic head bumps.
- Clean out/ repo backlog (~25 untracked log/json files) and extend .gitignore
  to cover session-run debug output. out/s91_coverage_plan.md kept (ROADMAP ref).
- Sync CLAUDE.md key numbers: 1898 unit (was 1886), 42 integration, alembic head
  037, ROADMAP.md added as single forward entry point.
- ROADMAP.md: S92 DONE, S93 DONE.

Closes v1.4.4 Consolidation sprint. PR cut + tag v1.4.4 next.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### LEPES 7 — v1.4.4 PR cut + tag

Csak ha a lint + unit + integration + e2e collect mind zold:

```bash
# 1. PR body frissites a S93 commit-tel
# (szerkeszd docs/v1.4.4_pr_description.md-t: S93 sor DONE, sign-off checklist teljesitve)

# 2. PR nyitas (ne feledd: user kerdezi meg a push-ot!)
git push -u origin feature/v1.4.4-consolidation
gh pr create --base main --title "v1.4.4 Consolidation Sprint (S88-S93)" \
   --body-file docs/v1.4.4_pr_description.md

# 3. Tag (csak merge utan!)
# git tag v1.4.4
# git push origin v1.4.4
```

> **STOP:** push / PR open / tag — ne tedd a user explicit engedelye nelkul. Ez a lepes a `/session-close` utan user-konzultacio.

### LEPES 8 — Session close

```
/session-close S93
```

---

## STOP FELTETELEK

- **HARD:** `test_alembic_034` dynamic head megoldas breaking-et okoz egy mas Phase 1b tesztben → rollback, scope-check, next session.
- **HARD:** `out/` cleanup kozben olyan fajlt talalsz ami mas terv-doku hivatkozik → ne torold, jelezd a usernek.
- **HARD:** A PR cut-nal kozbe jon uj merge-blocker (stale e2e, CI regresszio) → halt, scope a kovetkezo session-be.
- **SOFT:** `CLAUDE.md` kulcsszamok szamot sem tudjak felmerni automatikusan — ha gyanu, inkabb ne frissitsd a hibas szammal, hanem jelezd.
- **NINCS:** push vagy PR open a user kifejezett engedelye nelkul.

---

## SESSION VEGEN

```
/session-close S93
```

Utana a user dont:
- **Opcio A** — v1.4.4 PR open + merge + tag (user approve).
- **Opcio B** — S91.A coverage mini-sprint kezdes (emails.py + documents.py router tesztek, +2.7 p.p.).
- **Opcio C** — S94 Phase 1.5 kickoff (Vault hvac prod impl).

---

*Sprint H session: S93 = v1.4.4.6 (Consolidation Day 6 — sprint close housekeeping). Sprint H total: 6 session (S88-S93). After S93: Sprint H closes, v1.4.4 tag cut user-approval-pending.*
