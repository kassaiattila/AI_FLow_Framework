# AIFlow Sprint D — Session 48 Prompt (D0.5: Alembic 033 policy_overrides + PolicyEngine DB)

> **Datum:** 2026-04-15
> **Branch:** `feature/v2.0.0-phase-1a-foundation`
> **HEAD:** `c2678a5` (D0.4 ProviderRegistry + 4 ABC + contract tests)
> **Port:** API 8102 (dev), Frontend 5174 (dev)
> **Elozo session:** S47 — D0.4: ProviderMetadata Pydantic + 4 ABC (Parser/Classifier/Extractor/Embedder) + ProviderRegistry (12 methods) + 36 unit tests
> **Terv:** `01_PLAN/106_AIFLOW_v2_PHASE_1a_IMPLEMENTATION_GUIDE.md` (Section 5 Day 10, Week 2 finale)
> **Session tipus:** IMPLEMENTATION — Alembic migration + DB integration
> **Workflow:** Alembic 033 (policy_overrides table) → PolicyEngine.load_tenant_overrides_from_db() → PolicyOverrideRepository → Unit tests → Regression

---

## KONTEXTUS

### Honnan jottunk
```
Sprint A (v1.2.2): Infra + security + guardrails + audit           ✅ DONE
Sprint B (v1.3.0): E2E service excellence, 17 session              ✅ DONE
Sprint C (v1.4.0): UI Journey-First Refinement, 7 session          ✅ DONE (merged + tagged)
Sprint D (v2.0.0): Phase 1a Foundation                             🔄 IN PROGRESS
  S44: IntakePackage contracts + state machines                     ✅ DONE
  S45: Alembic migration 032 + IntakeRepository                    ✅ DONE
  S46: PolicyEngine + profile configs (33 fields, 26 tests)        ✅ DONE
  S47: ProviderRegistry + 4 ABC + contract tests (36 tests)        ✅ DONE
  S48: Alembic 033 policy_overrides + PolicyEngine DB               ← EZ A SESSION
```

### Hova tartunk — v2 Phase 1a (106_ Section 5)
```
Week 1: Contracts + State Machines (Day 1-5)              ✅ DONE (S44-S45)
Week 2: Policy + Provider Registry (Day 6-10)
  Day 6-7: PolicyEngine + profile config files             ✅ DONE (S46)
  Day 8-9: ProviderRegistry + 4 ABC                        ✅ DONE (S47)
  Day 10: Alembic 033 (policy_overrides) + DB integration   ← EZ A SESSION
Week 3: SkillInstance + Backward Compat (Day 11-15)
Week 4: Acceptance E2E + Docs + Demo (Day 16-20)
```

### Jelenlegi allapot
```
27 service | 175 endpoint | 48 DB tabla | 32 Alembic migration
1608 unit test | 169 E2E | 7 skill | 23 UI oldal
src/aiflow/intake/: package.py + state_machine.py + exceptions.py + repository.py
src/aiflow/policy/: __init__.py (PolicyConfig) + engine.py (PolicyEngine)
src/aiflow/providers/: __init__.py + metadata.py + interfaces.py + registry.py
config/profiles/: profile_a.yaml + profile_b.yaml
165 intake+policy+provider unit test (38 package + 37 state machine + 28 repository + 26 policy + 36 provider)
```

---

## ELOFELTETELEK (ELLENORIZNI ELOSZOR!)

```bash
# Branch ellenorzes
git branch --show-current          # Expected: feature/v2.0.0-phase-1a-foundation
git log --oneline -3               # Expected: HEAD = c2678a5

# Docker szolgaltatasok (PostgreSQL szukseges!)
docker ps | grep postgres          # Expected: running on port 5433

# Baseline smoke
python -m pytest tests/unit/ -x -q          # 1608+ PASS
python -m ruff check src/ tests/             # 0 error
cd aiflow-admin && npx tsc --noEmit          # 0 error

# Alembic aktualis allapot
PYTHONPATH=src alembic current               # Expected: 032 (head)
```

---

## FELADATOK

### LEPES 1: Alembic 033 — policy_overrides tabla

```
Cel: policy_overrides tabla letrehozasa a tenant-specifikus policy override-okhoz.

Fajl: src/aiflow/state/alembic/versions/033_policy_overrides.py

Forras: 106_ Section 5 Day 10

Tabla definicio:
  policy_overrides:
    override_id: UUID PK (server_default=gen_random_uuid())
    tenant_id: String(255) NOT NULL
    instance_id: String(255) NULLABLE  — opcionalis per-instance override
    policy_json: JSONB NOT NULL        — PolicyConfig mezo override-ok
    created_at: TIMESTAMP(tz) server_default=now()
    updated_at: TIMESTAMP(tz) server_default=now()

  Indexek:
    idx_policy_overrides_tenant — tenant_id
    uq_policy_overrides_tenant_instance — UNIQUE(tenant_id, instance_id)

FONTOS:
  - Kovetsd az existing migration minta (032_intake_tables.py)
  - downgrade() MUST drop the table
  - Alembic convention: depends_on = "032"
```

### LEPES 2: PolicyOverrideRepository

```
Cel: Async repository a policy_overrides tabla CRUD muveleteihez.

Fajl: src/aiflow/policy/repository.py

Metodusok:
  - async get_overrides_for_tenant(pool, tenant_id) -> dict[str, Any] | None
  - async get_overrides_for_instance(pool, tenant_id, instance_id) -> dict[str, Any] | None
  - async upsert_override(pool, tenant_id, policy_json, instance_id=None) -> UUID
  - async delete_override(pool, tenant_id, instance_id=None) -> bool

FONTOS:
  - asyncpg pool parameter (aiflow.api.deps.get_pool() pattern)
  - structlog logging
  - JSONB handling (json.dumps/loads)
  - Upsert via INSERT ... ON CONFLICT DO UPDATE
```

### LEPES 3: PolicyEngine DB integration

```
Cel: PolicyEngine bovitese DB-bol toltott tenant override-okkal.

Fajl: src/aiflow/policy/engine.py (bovites)

Uj metodus:
  @classmethod
  async def from_yaml_with_db(cls, profile_path, pool) -> PolicyEngine:
      """Load profile from YAML + all tenant overrides from DB."""

FONTOS:
  - Meglevo from_yaml() NE valtozzon (backward compat)
  - Az uj metodus az egyetlen async factory
  - get_for_tenant() mar mukodik dict-tel — a DB data is dict
```

### LEPES 4: Unit tesztek (10+)

```
Fajl: tests/unit/policy/test_repository.py

Minimum tesztek:
  - Alembic upgrade/downgrade (ha Docker fut)
  - PolicyOverrideRepository CRUD (get/upsert/delete)
  - from_yaml_with_db factory (mock pool fixture)
  - Upsert idempotency
  - tenant_id + instance_id unique constraint

MEGJEGYZES: Ha Docker/PostgreSQL nem fut, a repository teszteket
skip-eld @pytest.mark.skipif-vel. A unit tesztek NEM fugghetnek
Docker-tol — a DB teszteket integration/-be is teheted.
```

### LEPES 5: Lint + Regresszio + Commit

```bash
# Lint
python -m ruff check src/aiflow/policy/ tests/unit/policy/

# Alembic upgrade teszt (ha Docker fut)
PYTHONPATH=src alembic upgrade head

# Regresszio
python -m pytest tests/unit/ -x -q   # 1608 + ~10 uj = ~1618 PASS

# Commit
git add src/aiflow/state/alembic/versions/033_* src/aiflow/policy/repository.py src/aiflow/policy/engine.py tests/unit/policy/test_repository.py
git commit -m "feat(policy): D0.5 — Alembic 033 policy_overrides + PolicyEngine DB integration

Phase 1a Week 2 Day 10 (Week 2 finale):
- Alembic 033: policy_overrides table (tenant_id + instance_id + JSONB)
- PolicyOverrideRepository (get/upsert/delete async CRUD)
- PolicyEngine.from_yaml_with_db() async factory
- N unit tests

Source: 106_ Section 5 Day 10
Session: S48 | Sprint: D | Phase: 1a

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

## KOD REFERENCIAK

```
# OLVASD! — Ezeket a dokumentumokat KELL elolvasni:
01_PLAN/106_AIFLOW_v2_PHASE_1a_IMPLEMENTATION_GUIDE.md   — Section 5 Day 10
01_PLAN/101_AIFLOW_v2_COMPONENT_SPEC.md                  — N5 (PolicyEngine spec)

# Meglevo kod referenciak:
src/aiflow/policy/__init__.py          — PolicyConfig (33 fields)
src/aiflow/policy/engine.py            — PolicyEngine (from_yaml, get_for_tenant, is_allowed)
src/aiflow/intake/repository.py        — IntakeRepository minta (asyncpg CRUD pattern)
src/aiflow/state/alembic/versions/032_intake_tables.py — Migration minta
tests/unit/policy/test_engine.py       — Teszt strukturalasi minta
tests/unit/intake/test_repository.py   — Repository teszt minta
```

---

## STOP FELTETELEK

Ha BARMELYIK bekovetkezik → ALLJ meg es kerj iranymutatast:
- Architektura dontes szukseges (→ `/review` elobb)
- Docker/PostgreSQL nem elerheto es a migration nem tesztelheto
- Teszt FAIL amit 2 proba utan sem sikerul javitani
- Scope jelentosen nott a tervhez kepest
- Security concern (PII, auth, injection)

---

## SESSION VEGEN

```
1. /session-close S48    — validacio + commit + NEXT.md generalas
```

---

*Sprint D session: S48 = D0.5 (Phase 1a — Alembic 033 policy_overrides + PolicyEngine DB, Week 2 finale)*
