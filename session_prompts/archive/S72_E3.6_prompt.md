# AIFlow Sprint E — Session 72 Prompt (E3.6: Phase 1b PR open + merge gate + tag)

> **Datum:** 2026-05-08
> **Branch:** `feature/v1.4.1-phase-1b-sources`
> **HEAD:** `31880a5` (docs(phase_1b): E3.5 — Phase 1b architect Go/No-Go review (GO WITH CONDITIONS))
> **Port:** API 8102 | Frontend 5174
> **Elozo session:** S71 / E3.5 — architect Go/No-Go review: **GO WITH CONDITIONS**. Merge-blocking: C1 (file G5 regen GitHub issue), C2 (reviewer checklist on fresh Postgres). Post-merge tracked: C3–C6.
> **Terv:** `01_PLAN/session_S55_v1_4_1_phase_1b_sources_kickoff.md` (Exit criteria — "PR open + merge gate + tag")
> **Session tipus:** MERGE GATE (PR lifecycle — not implementation)

---

## KONTEXTUS

### Honnan jottunk
S71 (E3.5): fuggetlen architect review `out/phase_1b_architect_review.md`-ben, verdict **GO WITH CONDITIONS**. 5 kerdes × 5 PASS. G5 waiver elfogadva (stale `openapi.yaml`-nak nincs downstream fogyasztoja). G8 PARTIAL non-blocking (logging contract misalignment, nincs funkcionalis/biztonsagi gap). Phase 1a regression subprocess gate + numeric count assert erosebb mint CI matrix. Alembic 035 nullable semantically correct. Multi-tenant + rate limiting + HMAC gating mind tiszta.

### Hova tartunk — S72 / E3.6 merge gate
Ket merge-blocking feltetel C1 + C2 teljesitese, PR meginyitasa `feature/v1.4.1-phase-1b-sources` -> `main`, merge + tag `v1.4.1-phase-1b`.

- **C1** — File G5 OpenAPI regen ticket as GitHub issue (via `gh issue create`), link the issue in PR description.
- **C2** — Reviewer checklist validation: `docs/phase_1b_pr_description.md:140-149` test plan futtatasa fresh Postgres ellen (Docker-up + alembic upgrade head + 35 Phase 1b E2E + 199 Phase 1a regression subprocess).
- **PR open** — `gh pr create` with body from `docs/phase_1b_pr_description.md` (+ architect review link, + C1 issue link).
- **Merge + tag** — merge (squash or merge commit per team convention), tag `v1.4.1-phase-1b` push, switch branch back to main, pull.

### Jelenlegi allapot (E3.5 utan)
```
27 service | 176 endpoint | 49 DB tabla | 35 Alembic migration
1872 unit | 38 integration | 35 E2E Phase 1b | 199 E2E Phase 1a | 403 E2E osszesen
7 skill | 23 UI oldal | 5 source adapter (Email, File, Folder, Batch, API)
```

---

## ELOFELTETELEK

```bash
git branch --show-current        # feature/v1.4.1-phase-1b-sources
git log --oneline -3             # HEAD: 31880a5
gh auth status                   # gh CLI authenticated

.venv/Scripts/python.exe -m ruff check src/ tests/                # 0 error

ls out/phase_1b_architect_review.md                               # exists
ls out/phase_1b_acceptance_report.md                              # exists
ls docs/phase_1b_pr_description.md                                # exists
```

---

## FELADATOK

### LEPES 1: C1 — G5 OpenAPI regen GitHub issue

```
Cel:    Tracked GitHub issue a G5 remediation-nak (scope: post-merge 1 session).
Fajl:   `.github/ISSUE_TEMPLATE/*` (ha letezik) vagy inline body.
Forras: out/phase_1b_architect_review.md C3 + out/phase_1b_acceptance_report.md §G5 remediation.

gh issue create \
  --title "Phase 1c: OpenAPI yaml regen + CI diff gate (G5 remediation)" \
  --label "phase-1c,observability,documentation" \
  --body "<C3 scope from architect review: regenerate docs/api/openapi.yaml + docs/api/openapi.json via scripts/export_openapi.py, add CI job diffing live /openapi.json against checked-in file, prevent future drift. Estimated scope: 1 session (~0.5 days). Follow-up to Phase 1b merge (architect waiver accepted, see out/phase_1b_architect_review.md Q1/C3)."

Output: issue URL — save to variable, include in PR body.
```

### LEPES 2: C2 — Reviewer checklist on fresh Postgres

```
Cel:    Reprodukalhato validacio — docs/phase_1b_pr_description.md:140-149 listaja.
Fajlok: docker-compose.yml (pg:5433) + alembic + pytest tests.

make docker-up                                                                         # vagy: docker compose up -d postgres redis
PYTHONPATH=src .venv/Scripts/python.exe -m alembic upgrade head                        # vart: 035
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_1_phase_1b/ -v                       # 35 PASS
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_0_phase_1a/ -v                       # 199 PASS (regression)
.venv/Scripts/python.exe -m pytest tests/integration/ -v                               # 38 PASS
.venv/Scripts/python.exe -m pytest tests/unit/ -v                                      # 1872 PASS

Output: minden gate GREEN -> logfile mentes out/phase_1b_merge_gate_validation.log-ba
        (PR body reviewer evidence szekcio).
```

### LEPES 3: PR open

```
Cel:    `gh pr create` with full body.
Fajlok: docs/phase_1b_pr_description.md (body source).

# Body epites: docs/phase_1b_pr_description.md tartalom + prepend:
#   - "Architect review: out/phase_1b_architect_review.md (GO WITH CONDITIONS)"
#   - "G5 follow-up: <C1 issue URL>"
#   - "Reviewer checklist evidence: out/phase_1b_merge_gate_validation.log"

gh pr create \
  --base main \
  --head feature/v1.4.1-phase-1b-sources \
  --title "v1.4.1 Phase 1b — Source Adapters (5 adapters + N4 associator + multipart upload)" \
  --body-file <(cat docs/phase_1b_pr_description.md; echo; echo "## Architect review"; echo; echo "GO WITH CONDITIONS — see \`out/phase_1b_architect_review.md\`."; echo; echo "## Follow-ups"; echo; echo "- G5 OpenAPI regen: #<C1 issue number>"; echo "- C4-C6 tracked in architect review, Phase 1c scope.")

Output: PR URL. Record in session summary.
```

### LEPES 4: Merge + tag

```
Cel:    PR merge, tag push, branch cleanup.

# WAIT: user-approval required? Ha team convention es git config szerint automerge lehet:
gh pr merge --squash --delete-branch                                                   # vagy --merge

# Ha merge success:
git checkout main && git pull
git tag -a v1.4.1-phase-1b -m "AIFlow v1.4.1 Phase 1b: 5 source adapters + N4 associator + multipart upload"
git push origin v1.4.1-phase-1b

Output: tag URL (github.com/.../releases/tag/v1.4.1-phase-1b).
```

**STOP** LEPES 4 elott, ha a team konvencio user-approval-t ir elo a PR merge-re — akkor ezt a lepest a user vegzi, a session csak a PR URL-ig megy.

### LEPES 5: CLAUDE.md frissites + session close

```bash
# CLAUDE.md: Phase 1b "COMPLETE" -> "MERGED" state, branch pointer main-re.
# 01_PLAN/session_S55_*: Phase 1b status -> MERGED, tag: v1.4.1-phase-1b.

git add CLAUDE.md 01_PLAN/session_S55_v1_4_1_phase_1b_sources_kickoff.md [egyeb]
git commit -m "docs(phase_1b): E3.6 — Phase 1b merged (tag v1.4.1-phase-1b)"
git push

/session-close E3.6
```

---

## STOP FELTETELEK

ALLJ MEG es kerj iranymutatast, ha:
- `gh` CLI nincs authenticated (`gh auth login` user-interactive).
- Fresh Postgres reviewer checklist (LEPES 2) barmilyen teszt FAIL-el -> merge blokkolva, investigate.
- PR creation fail (pl. branch protection rule hianyzo review approval) -> user kezelje.
- Team konvencio szerint user-approval kell a merge-re -> megallsz a PR URL-nel, atadsz.
- Merge conflict `main`-nel -> rebase vagy user-consultation (ne auto-resolve).
- Tag push fail (permission / protected tag) -> user-interactive.

---

## SESSION VEGEN

```
/session-close E3.6
```

Utana:
- Phase 1b MERGED — S73 = Phase 1c kickoff (association backfill + C4 observability harden + C5 NOT NULL constraint, per architect review).
- VAGY: ha S72 csak PR-nyitasig jut (user-merge), S73 = Phase 1c kickoff miutan a user megergesitette a merge-et.

---

*Sprint E session: S72 = E3.6 (Phase 1b PR open + merge gate + tag v1.4.1-phase-1b)*
