# AIFlow Sprint E — Session 65 Prompt (E2.4: Week 2 Day 10 — Exit Gate, 5-adapter coexist E2E)

> **Datum:** 2026-05-01 (Week 2 Day 10)
> **Branch:** `feature/v1.4.1-phase-1b-sources`
> **HEAD:** `4eccb5b` (feat(sources): E2.3-B — FastAPI webhook router + 8 integration tests)
> **Port:** API 8102 | Frontend 5174
> **Elozo session:** S64 / E2.3-B — FastAPI webhook router wiring ApiSourceAdapter, 8 integration tests (202/401/409/413/OpenAPI schema), ruff 0, 199 Phase 1a E2E PASS.
> **Terv:** `01_PLAN/session_S55_v1_4_1_phase_1b_sources_kickoff.md` (Week 2 Day 10 — E2.4 exit gate)
> **Session tipus:** TESTING (un-skip E2E + ABC contract parametrization + OpenAPI diff review)

---

## KONTEXTUS

### Honnan jottunk
Week 2 Days 6-9 DONE:
- Day 6 (E2.1): `FolderSourceAdapter` — watchdog + debounce + mid-write guard + 19 unit tests
- Day 7 (E2.2): `BatchSourceAdapter` — ZIP/tar + zip-bomb guard + 23 unit tests
- Day 8 (E2.3-A): `ApiSourceAdapter` core — HMAC-SHA256 + replay + idempotency + 26 unit tests
- Day 9 (E2.3-B): `POST /api/v1/sources/webhook` router + 8 integration tests

Five adapters live: Email (IMAP + Outlook COM backends), File, Folder, Batch, Api.

### Hova tartunk — Week 2 Day 10 (E2.4)
**Week 2 exit gate** — prove the 5 adapters coexist and conform to one contract:
- Un-skip 3 Phase 1b E2E suites: `test_folder_source.py`, `test_batch_source.py`, `test_api_source.py` (min 6 tests each — success + rejection + edge).
- Parametrized **ABC contract test** over ALL 5 adapters: same fixture proves each one honors `SourceAdapter` ABC (metadata shape, fetch_next/ack/reject lifecycle, health_check).
- **OpenAPI diff review** — capture schema snapshot for `/api/v1/sources/webhook`, compare vs. baseline, confirm only additive changes.

Week 3 (E3.x) starts after this exit gate (association modes N0-N4).

### Jelenlegi allapot (E2.3-B utan)
```
27 service | 176 endpoint | 49 DB tabla | 34 Alembic migration
1837 unit | 8 integration | 199 E2E Phase 1a | 16 E2E Phase 1b collect (currently skipped where listed)
7 skill | 23 UI oldal | 5 source adapter (Email [IMAP+Outlook], File, Folder, Batch, Api)
```

---

## ELOFELTETELEK

```bash
git branch --show-current        # feature/v1.4.1-phase-1b-sources
git log --oneline -3             # HEAD: 4eccb5b

.venv/Scripts/python.exe -m pytest tests/unit/sources/ -q            # 163 PASS
.venv/Scripts/python.exe -m pytest tests/integration/sources/ -q     # 8 PASS
.venv/Scripts/python.exe -m ruff check src/ tests/                   # 0 error
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_0_phase_1a/ -q     # 199 PASS

# ABC import sanity
.venv/Scripts/python.exe -c "from aiflow.sources import EmailSourceAdapter, FileSourceAdapter, FolderSourceAdapter, BatchSourceAdapter, ApiSourceAdapter; print('5 adapters import OK')"
```

---

## FELADATOK

### LEPES 1: Un-skip `test_folder_source.py`

```
Cel:    E2E ≥6 teszt FolderSourceAdapter-re (success + rejection + edge)
Fajlok: tests/e2e/v1_4_1_phase_1b/test_folder_source.py
Forras: 01_PLAN/session_S55_*.md Day 10

Scenariok:
  - [ ] happy path: fajl drop → debounce window → IntakePackage yield
  - [ ] mid-write guard: nyitott fajl (growing) → NEM yield amig stabil
  - [ ] stabilizalodas utan yield (size unchanged N poll)
  - [ ] rejection: sanitizalt filename path-traversal kiserlet
  - [ ] edge: >max_package_bytes → SourceAdapterError
  - [ ] health_check: writable storage_root → True
```

### LEPES 2: Un-skip `test_batch_source.py`

```
Cel:    E2E ≥6 teszt BatchSourceAdapter-re
Fajlok: tests/e2e/v1_4_1_phase_1b/test_batch_source.py

Scenariok:
  - [ ] happy path: ZIP kicsomagolas → multi-file IntakePackage
  - [ ] tar.gz kezelese
  - [ ] zip-bomb guard (compression ratio limit) → SourceAdapterError
  - [ ] path-traversal archive entry → reject
  - [ ] empty archive → SourceAdapterError vagy ures package
  - [ ] >max_package_bytes → SourceAdapterError
```

### LEPES 3: Un-skip `test_api_source.py`

```
Cel:    E2E ≥6 teszt ApiSourceAdapter-re (pure adapter level — NEM router!)
Fajlok: tests/e2e/v1_4_1_phase_1b/test_api_source.py

Scenariok:
  - [ ] enqueue valid signature → IntakePackage
  - [ ] enqueue invalid signature → SourceAdapterError
  - [ ] enqueue replay window over → SourceAdapterError
  - [ ] duplicate idempotency_key → SourceAdapterError
  - [ ] fetch_next/acknowledge lifecycle
  - [ ] fetch_next/reject lifecycle
```

### LEPES 4: Parametrized ABC contract test

```
Cel:    Egy test parametrizalva mind az 5 adapter-rel, igazolja a
        SourceAdapter ABC-t (metadata shape, fetch_next/ack/reject,
        health_check, source_type ClassVar).
Fajl:   tests/integration/sources/test_adapter_contract.py (uj)

Minta:
  @pytest.mark.parametrize("factory", [
      email_factory, file_factory, folder_factory, batch_factory, api_factory,
  ])
  def test_adapter_conforms_to_abc(factory): ...

Minden factory fixture egy mar enqueue-olt IntakePackage-et allit elo,
igy az ACK/REJECT lifecycle tesztelheto deterministic modon.
```

### LEPES 5: OpenAPI diff review

```
Cel:    Webhook endpoint schema snapshot es compare
Parancsok:
  .venv/Scripts/python.exe -c "from aiflow.api.app import create_app; import os, json; \
    os.environ.setdefault('AIFLOW_WEBHOOK_HMAC_SECRET', 'placeholder'); \
    app = create_app(); print(json.dumps(app.openapi()['paths']['/api/v1/sources/webhook'], indent=2))" \
    > out/webhook_openapi_snapshot.json

Ellenorizd:
  - HMAC secret NEM jelenik meg sehol
  - 4xx response-ok dokumentaltak (400, 401, 409, 413)
  - X-Webhook-Signature / X-Webhook-Timestamp / X-Filename header parameterek
  - 202 success schema = WebhookAcceptedResponse
```

### LEPES 6: Validacio + commit

```bash
.venv/Scripts/python.exe -m pytest tests/unit/sources/ -q                      # 163 PASS
.venv/Scripts/python.exe -m pytest tests/integration/sources/ -q               # >=8 + ABC contract PASS
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_1_phase_1b/test_folder_source.py tests/e2e/v1_4_1_phase_1b/test_batch_source.py tests/e2e/v1_4_1_phase_1b/test_api_source.py -q  # 3x6=18+ PASS
.venv/Scripts/python.exe -m ruff check src/ tests/                             # 0 error
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_0_phase_1a/ -q               # 199 PASS (regresszio)

/session-close E2.4
```

---

## STOP FELTETELEK

ALLJ MEG es kerj iranymutatast, ha:
- Egy adapter teszt konkret backing service-t igenyel (pl. Outlook COM) a CI-ben → quarantine marker + FIXME ticket, ne blokkold az exit gate-et
- Az ABC contract parametrization >2x a budget → split uj E2.5 session-be (Week 2 exit-et marad a 3 un-skip + OpenAPI diff)
- OpenAPI baseline nem letezik → capture snapshot-ot Day 10-en + commit `out/webhook_openapi_snapshot.json` -t baseline-kent, legkozelebb diff-elheto
- Phase 1a regresszio FAIL (199 elvart) → STOP, root-cause debug, ne folytasd Week 3-mal

---

## SESSION VEGEN

```
/session-close E2.4
```

Utana S66 / E3.1 = Week 3 Day 11 (association modes N0-N4 kickoff, `association_mode` DB oszlop hozzaadasa Alembic 35-tel).

---

*Sprint E session: S65 = E2.4 (Phase 1b Week 2 Day 10 — exit gate: 5-adapter coexist, ABC contract test, OpenAPI diff review)*
