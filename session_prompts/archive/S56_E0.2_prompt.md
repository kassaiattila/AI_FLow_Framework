# AIFlow Sprint E — Session 56 Prompt (E0.2: Week 1 Day 1 — Source registry + Alembic 034)

> **Datum:** 2026-04-22 (Week 1 Day 1)
> **Branch:** `feature/v1.4.1-phase-1b-sources`
> **HEAD:** `bce4f90` (feat(sources): E0.1 — Phase 1b kickoff — SourceAdapter ABC + E2E skeleton)
> **Port:** API 8102 | Frontend 5174
> **Elozo session:** S55 / E0.1 — SourceAdapter ABC (12 contract tests) + 7 E2E placeholders (13 tests skipped) + sprint plan finalized (Day 1-15 + G1-G9 gates)
> **Terv:** `01_PLAN/session_S55_v1_4_1_phase_1b_sources_kickoff.md` (Week 1 Day 1 szekcio)
> **Session tipus:** IMPLEMENTATION (registry wiring + DB migration + shared E2E fixtures)

---

## KONTEXTUS

### Honnan jottunk
E0.1 (S55) lezarta a Phase 1b kickoff-ot:
- `src/aiflow/sources/base.py` — `SourceAdapter` ABC + `SourceAdapterMetadata` Pydantic descriptor
- `tests/unit/sources/test_base.py` — 12 contract test PASS (ABC enforcement, metadata shape, lifecycle)
- `tests/e2e/v1_4_1_phase_1b/` — 7 file, 13 skipped placeholder
- `01_PLAN/session_S55_*` — sprint plan finalizalva (Day 1-15 + Phase 1b G1-G9 gate checklist)

Phase 1a (v1.4.0) `main`-en, tag `v1.4.0-phase-1a`, 199 Phase 1a E2E PASS (regression-mentes).

### Hova tartunk — Week 1 Day 1 (E0.2)
Shared infrastruktura az 5 source adapterhez:
- `SourceAdapterRegistry` (analog `ProviderRegistry`-hez) — source_type szerinti lookup
- Alembic `034_source_type.py` — `intake_packages.source_type` default backfill + CHECK constraint hardening
- E2E `conftest.py` bovitese — valos tenant + storage root + IntakeRepository fixture-ok

### Jelenlegi allapot (E0.1 utan)
```
27 service | 175 endpoint | 49 DB tabla | 33 Alembic migration (032+033 Phase 1a)
1686 unit (1674 pre + 12 sources ABC) | 383 E2E (169 pre + 199 Phase 1a + 15 Phase 1b skip)
7 skill | 23 UI oldal
```

---

## ELOFELTETELEK

```bash
git branch --show-current        # feature/v1.4.1-phase-1b-sources
git log --oneline -3             # HEAD: bce4f90 (E0.1 kickoff)

# Docker services fel (DB migraciohoz KELL):
docker compose up -d postgres redis

# Baseline:
.venv/Scripts/python.exe -m pytest tests/unit/ -x -q                  # 1686 PASS
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_0_phase_1a/ -q      # 199 PASS
.venv/Scripts/python.exe -m ruff check src/ tests/                    # 0 error
PYTHONPATH=src .venv/Scripts/python.exe -m alembic current            # 033_policy_overrides
```

---

## FELADATOK

### LEPES 1: SourceAdapterRegistry

```
Cel:    `src/aiflow/sources/registry.py` — source_type → SourceAdapter class lookup.
Forras: src/aiflow/providers/registry.py (template), E0.1 base.py
Fajlok:
  - src/aiflow/sources/registry.py       (uj)
  - src/aiflow/sources/__init__.py       (export SourceAdapterRegistry)
  - tests/unit/sources/test_registry.py  (uj, >=10 teszt)

API:
  - register(adapter_cls: type[SourceAdapter]) -> None
  - get(source_type: IntakeSourceType) -> type[SourceAdapter]
  - list_all() -> list[SourceAdapterMetadata]
  - Raises DuplicateAdapterError ha ugyanaz a source_type ketszer regisztralva
  - Raises UnknownSourceTypeError ha ismeretlen source_type-ot kernek

Exception osztalyok: src/aiflow/sources/exceptions.py (uj).
```

### LEPES 2: Alembic 034 — source_type hardening

```
Cel:    intake_packages.source_type oszlop hardening (Phase 1a meg nullable volt).
Fajl:   alembic/versions/034_source_type.py

Upgrade:
  - UPDATE intake_packages SET source_type = 'legacy' WHERE source_type IS NULL;
  - ALTER COLUMN source_type SET NOT NULL;
  - ADD CONSTRAINT ck_intake_source_type CHECK (source_type IN
      ('email','file_upload','folder_import','batch_import','api_push','legacy'));

Downgrade:
  - DROP CONSTRAINT ck_intake_source_type;
  - ALTER COLUMN source_type DROP NOT NULL;

Teszt:
  - tests/e2e/v1_4_1_phase_1b/test_alembic_034.py (uj, un-skipped)
    + upgrade -> downgrade -> upgrade round-trip
    + legacy backfill helyesseg
```

### LEPES 3: E2E conftest — shared fixtures

```
Cel:    Week 1 Day 2+ (email/file adapter E2E) barmikor tenant + IntakeRepository-hoz nyuljon.
Fajl:   tests/e2e/v1_4_1_phase_1b/conftest.py (bovites)

Uj fixture-ok:
  - phase_1b_tenant_id        (mar letezik E0.1-bol)
  - phase_1b_storage_root     (tmp_path_factory, per-session)
  - phase_1b_db_pool          (shared asyncpg pool, get_pool() pattern per CLAUDE.md)
  - phase_1b_intake_repository(IntakeRepository instance, pool-ra kotve)
  - phase_1b_policy_engine    (PolicyEngine, profile_a default)
  - phase_1b_source_registry  (friss SourceAdapterRegistry minden teszthez)

FONTOS (CLAUDE.md feedback_asyncpg_pool_event_loop):
  asyncpg pool-ok event-loop-bound. Hasznald aiflow.api.deps.get_pool()-t,
  ne per-function fixture-t.
```

### LEPES 4: Validacio + commit

```bash
.venv/Scripts/python.exe -m ruff check src/ tests/
.venv/Scripts/python.exe -m pytest tests/unit/sources/ -v              # >=22 PASS (12+10 new)
.venv/Scripts/python.exe -m pytest tests/unit/ -x -q                   # 1696+ PASS
.venv/Scripts/python.exe -m pytest tests/e2e/v1_4_1_phase_1b/ -q       # >=1 un-skipped (alembic 034)
PYTHONPATH=src .venv/Scripts/python.exe -m alembic upgrade head        # 034 alkalmazva
PYTHONPATH=src .venv/Scripts/python.exe -m alembic downgrade -1        # round-trip
PYTHONPATH=src .venv/Scripts/python.exe -m alembic upgrade head

git add src/aiflow/sources/registry.py \
        src/aiflow/sources/exceptions.py \
        src/aiflow/sources/__init__.py \
        tests/unit/sources/test_registry.py \
        tests/e2e/v1_4_1_phase_1b/conftest.py \
        tests/e2e/v1_4_1_phase_1b/test_alembic_034.py \
        alembic/versions/034_source_type.py

# Conventional commit (HEREDOC-ban) -> /session-close E0.2
```

---

## STOP FELTETELEK

ALLJ MEG es kerj iranymutatast, ha:
- PostgreSQL nem inditható Docker-ben (migration blokkolva)
- 199 Phase 1a E2E regresszio (E0.1 commit elrontott valamit)
- Alembic 034 downgrade nem round-trip-tiszta (adatveszteseg)
- `source_type` backfill legacy erteke utkozik meglevo CHECK-kel (plan modositas kell)
- Scope >2x — ha a registry + migration + fixtures osszesen >1 session, bontsd ket sessionre (E0.2 = registry + migration, E0.3 = fixtures)

---

## SESSION VEGEN

```
/session-close E0.2
```

Utana S57 / E1.1 = Week 1 Day 2 (EmailSourceAdapter IMAP backend).

---

*Sprint E session: S56 = E0.2 (Phase 1b Week 1 Day 1 — registry + Alembic 034 + shared fixtures)*
