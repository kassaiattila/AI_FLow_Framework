# v1.4.4 Consolidation — Sprint H PR

> **Status:** READY TO OPEN. Finalized at S93 session-close (2026-04-28 / 2026-04-19 local).
> **Base:** `main` @ tag `v1.4.3-phase-1d` (merge commit `0d669aa`).
> **Branch:** `feature/v1.4.4-consolidation`.
> **Target tag:** `v1.4.4` (cut after merge).
> **HEAD:** `ea348bb chore(session-close): autonomize validation + commit + NEXT.md flow`.
> **Post-S93 housekeeping:** 3 additional commits folded in — `c7ee52e` (archive S93 prompt + queue S94 NEXT.md), `d79a7c2` (repo hygiene: gitignore DOHA/ + scheduled_tasks.lock, share settings.local.json team permissions), `ea348bb` (session-close autonomize). No code/tests/migrations touched by these three.

---

## Summary

Non-feature consolidation sprint. Closes drift accumulated during Phase 1a–1d, hardens
a round-trip Alembic contract test, cleans the working tree, and **authors a
use-case-first replan** (`01_PLAN/110_USE_CASE_FIRST_REPLAN.md`) that governs v1.4.5
onwards: every sprint from here ships exactly one end-user use-case going end-to-end
green (document processing / RAG chat / email intent), monitored in Langfuse, visible
in admin UI.

No DB migrations in this PR. No breaking API changes. Coverage gate remains at
the pre-sprint waiver (issue #7 stays open — the S91.A–D mini-sprint plan is
absorbed into sprint-by-sprint new-test targets per the replan).

Six sessions (S88–S93) land as consecutive commits on one branch and ship as one PR.

---

## Session breakdown

| Session | Commit | Scope | Status |
|---|---|---|---|
| S88 / v1.4.4.1 | `82d0cd8` | Version reconcile (`_version.py` 1.3.0 → 1.4.4, `pyproject.toml` 1.4.0 → 1.4.4), port doc fix (5174 ref → 5173 reality), head-relative alembic assertions (interim), stale session-prompt archive (~25 files) | DONE |
| S89 / v1.4.4.2 | `713f122` | Frontend dev-env verified, `01_PLAN/ADR-UI-Library.md` created (Untitled UI path enforced) | DONE |
| S90 / v1.4.4.3 | `ea72c6b` | Journey E2E rerun + aiflow-admin contract regression fixes | DONE |
| S91 / v1.4.4.4 | `d69e256` | `test_auth` module-level `patch.start()` leak fix (fixture-scoped), 3 e2e files `pytest.mark.asyncio` explicit markers, `out/s91_coverage_plan.md` (4-session coverage roadmap after 13 p.p. HARD STOP) | DONE |
| S92 / v1.4.4.5 | `e687681` | `01_PLAN/ROADMAP.md` (v1 forward queue), `104_*` §7.2 / §8.1 / §10.3 drift fix (Phase 1a scope correction — 3 contracts + 1 state machine actually delivered, 10 + 6 deferred), this PR draft | DONE |
| S93 / v1.4.4.6 | `07e2fc3` + `5bb5de9` | **`test_alembic_034` ScriptDirectory head-relative hardening + `out/` log cleanup (18 orphan artifacts) + `.gitignore` out/ scratch patterns + `CLAUDE.md` key-numbers sync (1898 unit, 410 E2E, head 037). Then `01_PLAN/110_USE_CASE_FIRST_REPLAN.md` + ROADMAP.md rewrite + S94 kickoff prompt for UC1 document processing sprint.** | DONE |

---

## What's in this PR

### Documentation (largest surface)

- `01_PLAN/110_USE_CASE_FIRST_REPLAN.md` — **new** master plan for v1.4.5–v1.4.8.
  3 in-scope use-cases (UC1 document processing, UC2 RAG chat, UC3 email intent),
  archived skills (cubix, spec_writer) explicitly out of scope. Approved processing
  flow (IntakePackage → Routing → Parser [Unstructured / PyMuPDF4LLM / Docling std /
  Docling VLM / Azure DI] → Classifier → Extractor → Embedder). Working assumptions:
  LangChain opt-in utility layer (≤3 importers), Azure DI Profile B only, Unstructured
  dual-role parser+chunker, Langfuse as pipeline + prompt admin backend. Per-sprint
  acceptance matrix + blocker maps.
- `01_PLAN/ROADMAP.md` — **rewritten** as use-case-first queue. v1.4.4 Sprint H
  closed; v1.4.5 Sprint I (doc) → v1.4.6 Sprint J (RAG) → v1.4.7 Sprint K (email) →
  v1.4.8 Sprint L (monitoring + cost) queued with session-level scope. Vault +
  self-hosted Langfuse moved to v1.4.9 (standalone full sprint; the prior "0.5 sprint"
  estimate was unrealistic). Phase 2 sub-phases gated behind per-provider PP0 spike.
  Cross-cutting backlog trimmed (test_alembic_034, out/ cleanup, CLAUDE.md sync all
  retired as DONE).
- `01_PLAN/104_AIFLOW_v2_FINAL_MASTER_INDEX.md` — §7.2 / §8.1 scope correction
  (Phase 1a delivered 3 intake contracts + 1 state machine, not 13 + 7). §10.3 lists
  10 deferred v2 contracts + 6 deferred state machines with owning Phase + pre-work
  anchors.
- `01_PLAN/ADR-UI-Library.md` — Untitled UI path enforced (S89).
- `CLAUDE.md` — Key Numbers synced to current state (1898 unit, 410 E2E including
  7 Phase 1d, Alembic head 037). Current Plan section now points to
  `01_PLAN/ROADMAP.md` as single forward entry point.
- `session_prompts/S94_v1.4.5.1_prompt.md` — **new** kickoff prompt for Sprint I
  first session (`extract_from_package()` impl + Docling standard parser provider +
  ExtractionResult/ParserResult Pydantic v1 stubs + PolicyEngine gate).
- `session_prompts/archive/S62_*` through `S92_*` — prompts moved out of
  `session_prompts/` root.
- `docs/v1.4.4_pr_description.md` — this file.

### Code / config

- `src/aiflow/_version.py` — `1.3.0` → `1.4.4`.
- `pyproject.toml` — version aligned.
- `aiflow-admin/vite.config.ts` — port reference reconciled.
- `.gitignore` — `out/` scratch-pad patterns added (api_log*, cli_test.*, *.log,
  token.*, spec_eval.json, diagram_eval.json, hardening_*.md,
  phase_1b_merge_gate_*.log, s89_*.log, s90_*.log, mypy_*.log).
- `tests/unit/security/test_auth.py` — module-level `patch.start()` moved into
  fixture to eliminate cross-test leak (S91).
- `tests/e2e/v1_4_1_phase_1b/test_alembic_034.py` — head assertion now resolves
  via `alembic.script.ScriptDirectory` (single source of truth = migration folder,
  not DB row); asserts DB revision == script head before round-trip (S93).
- 3 e2e files — explicit `@pytest.mark.asyncio` markers (S91).
- `tests/e2e/test_journey_*` — aiflow-admin journey E2E assertions updated (S90).
- `src/aiflow/api/v1/intake/upload_package.py` + `tests/integration/...` — minor
  contract regressions from S90.
- `out/` — 18 orphan debug artifacts removed; `b6_validation_round{1,2}.md` kept
  and tracked (referenced from `01_PLAN/63_UI_USER_JOURNEYS.md`).

---

## Test evidence (final, HEAD `5bb5de9`)

- **Ruff:** `ruff check src/ tests/` → exit 0.
- **Unit:** 1898 / 1898 green (`pytest tests/unit/ -x -q --no-cov`, ~33 s).
- **Integration:** 42 / 42 green (~20 s).
- **Phase 1a E2E:** 199 / 199 green (historic baseline).
- **Phase 1b E2E:** 35 / 35 green (historic baseline).
- **Phase 1d E2E:** 7 / 7 green (historic baseline).
- **`test_alembic_034`:** PASS (new ScriptDirectory-based assertion).
- **Total E2E collected:** 410 (169 pre-existing + 199 Phase 1a + 35 Phase 1b + 7 Phase 1d).
- **Coverage:** 67.00 % (baseline, issue #7 OPEN). Gate stays at pre-sprint waiver;
  replan absorbs S91.A–D into sprint-by-sprint targets with coverage flip at v1.4.8.

---

## Breaking changes

None.

---

## Migrations

None. Alembic head stays at `037` across this sprint (verified S88, unchanged since Phase 1c).

---

## Deferred / follow-up (post-merge)

All follow-up is governed by `01_PLAN/ROADMAP.md` now.

- **v1.4.5 Sprint I — UC1 document processing** (5 sessions, S94–S98). First
  session prompt ready at `session_prompts/S94_v1.4.5.1_prompt.md`. Cuts a fresh
  branch `feature/v1.4.5-doc-processing` from `main` after this PR's tag.
- **v1.4.6 Sprint J — UC2 RAG chat** (5 sessions, S99–S103).
- **v1.4.7 Sprint K — UC3 email intent** (5 sessions, S104–S108).
- **v1.4.8 Sprint L — cross-cutting monitoring + cost** (3 sessions, S109–S111).
  Coverage gate flip to 80 % happens here.
- **v1.4.9** — Vault `hvac` prod + self-hosted Langfuse + Profile A air-gapped E2E
  (was v1.4.5 0.5-sprint estimate; promoted to standalone full sprint).
- **Phase 2 (v1.5.0–v1.5.3)** — per-provider PP0 spike gates each sub-phase.
- **§10.3 deferred contracts** — 7 remaining after v1.4.8; each lands in its
  owning Phase 2/3 sub-sprint.

---

## Risk

- **Code risk:** low. Only change with runtime surface is the `test_alembic_034`
  assertion tightening (S93) — round-tripped against real Postgres, green.
- **Process risk:** low. The replan (110_*) is a forward document; it reshapes
  subsequent sprint ordering but does not retroactively invalidate any DONE scope.
- **Rollback:** revert commits in order S93-b (`5bb5de9`) → S93-a (`07e2fc3`) →
  S92 → S91 → S90 → S89 → S88 on the feature branch. No state outside the repo
  changes.

---

## Sign-off checklist

- [x] S88–S93 committed.
- [x] `ruff` + `pytest tests/unit/` + `pytest tests/integration/` green on HEAD.
- [x] `test_alembic_034` PASS with ScriptDirectory-based head assertion.
- [x] `out/` backlog cleaned (18 orphan artifacts removed, 2 plan-referenced kept).
- [x] `CLAUDE.md` key numbers match test evidence (1898 unit, 410 E2E, head 037).
- [x] `01_PLAN/ROADMAP.md` reflects Sprint H closed + Sprint I queued.
- [x] `01_PLAN/110_USE_CASE_FIRST_REPLAN.md` authored and linked from ROADMAP.
- [x] `session_prompts/S94_v1.4.5.1_prompt.md` ready.
- [ ] PR opened to `main` (pending user action).
- [ ] Tag `v1.4.4` created after merge (pending user action).

---

*Generated 2026-04-28, S93 / v1.4.4.6 session-close. Replaces the S92 DRAFT.*
