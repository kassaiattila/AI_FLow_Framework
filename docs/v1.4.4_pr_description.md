# v1.4.4 Consolidation — Sprint H PR draft

> **Status:** DRAFT (not yet opened). Prepared in S92 (2026-04-27). Final cut at S93 session-close.
> **Base:** `main` @ tag `v1.4.3-phase-1d` (merge commit `0d669aa`).
> **Branch:** `feature/v1.4.4-consolidation`.
> **Target tag:** `v1.4.4` (after S93 merges).

---

## Summary

Non-feature consolidation sprint. Closes drift accumulated during Phase 1a–1d and queues the
forward roadmap that the next auto-sprint will consume. No DB migrations. No breaking API
changes. Coverage gate remains at the pre-sprint waiver (issue #7 stays open — separate
mini-sprint plan recorded).

Six sessions (S88–S93) land as consecutive commits on one branch and ship as one PR.

---

## Session breakdown

| Session | Commit | Scope | Status |
|---|---|---|---|
| S88 / v1.4.4.1 | `82d0cd8` | Version reconcile (`_version.py` 1.3.0 → 1.4.4, `pyproject.toml` 1.4.0 → 1.4.4), port doc fix (5174 ref → 5173 reality), head-relative alembic assertions, stale session-prompt archive (~25 files) | DONE |
| S89 / v1.4.4.2 | `713f122` | Frontend dev-env verified, `01_PLAN/ADR-UI-Library.md` created (Untitled UI path enforced) | DONE |
| S90 / v1.4.4.3 | `ea72c6b` | Journey E2E rerun + aiflow-admin contract regression fixes | DONE |
| S91 / v1.4.4.4 | `d69e256` | `test_auth` module-level `patch.start()` leak fix (fixture-scoped), 3 e2e files `pytest.mark.asyncio` explicit markers, `out/s91_coverage_plan.md` (4-session coverage roadmap after 13 p.p. HARD STOP) | DONE |
| **S92 / v1.4.4.5** | _this commit_ | `01_PLAN/ROADMAP.md` (forward queue), `104_*` §7.2 / §8.1 / §10.3 drift fix (Phase 1a scope correction — 3 contracts + 1 state machine actually delivered), this PR draft | DONE |
| S93 / v1.4.4.6 | _pending_ | `test_alembic_034` head assertion drift fix (035 → 037 → head-relative), `out/` log cleanup, `CLAUDE.md` key-numbers sync | QUEUED |

---

## What's in this PR

### Documentation (largest surface)

- `01_PLAN/ROADMAP.md` — **new** forward queue (Sprint H + coverage mini-sprint + v1.4.5 Phase 1.5 + Phase 2 prep + Phase 2a-e + Phase 3 + optional Phase 4). Single-source pointer for `/auto-sprint` and `/next`.
- `01_PLAN/104_AIFLOW_v2_FINAL_MASTER_INDEX.md` — §7.2 and §8.1 scope correction (Phase 1a actually delivered 3 intake Pydantic contracts + 1 IntakeStateMachine, not the originally-announced 13 + 7). New §10.3 lists the 10 deferred v2 contracts and 6 deferred state machines with owning Phase + pre-work session anchors.
- `01_PLAN/ADR-UI-Library.md` — Untitled UI path enforced (S89).
- `docs/v1.4.4_pr_description.md` — this file.
- `session_prompts/archive/S62_*` through `S79_*` — ~25 files moved out of `session_prompts/` root.

### Code / config

- `src/aiflow/_version.py` — `1.3.0` → `1.4.4`.
- `pyproject.toml` — version aligned.
- `aiflow-admin/vite.config.ts` — port reference reconciled.
- `tests/unit/security/test_auth.py` — module-level `patch.start()` moved into fixture to eliminate cross-test leak (S91).
- `tests/e2e/v1_4_1_phase_1b/test_alembic_034.py` — head assertion adjusted (S88 head-relative); final hardening lands in S93.
- 3 e2e files — explicit `@pytest.mark.asyncio` markers.
- `tests/e2e/test_journey_*` — aiflow-admin journey E2E assertions updated.
- `src/aiflow/api/v1/intake/upload_package.py` + `tests/integration/...` — minor contract regressions from S90.

---

## Test evidence

- **Unit + integration:** 1940 / 1940 green (108 s, S91 run).
- **Phase 1a E2E:** 199 / 199 green.
- **Phase 1b E2E:** 35 / 35 green.
- **Total E2E:** 403 collected (169 pre-existing + 199 Phase 1a + 35 Phase 1b).
- **Coverage:** 67.00 % (baseline, issue #7 OPEN). Gate stays at pre-sprint waiver.

---

## Breaking changes

None.

---

## Migrations

None. Alembic head stays at `037` across this sprint (verified S88, unchanged since Phase 1c).

---

## Deferred / follow-up

- **Coverage mini-sprint (S91.A–D):** 4-session plan in `out/s91_coverage_plan.md`. Targets emails + documents routers (A), RAG + pipelines (B), email_connector + runner services (C), vectorstore + tools + auth hardening + gate flip (D). Closes issue #7 at S91.D end.
- **Phase 1.5 (S94–S95):** Vault `hvac` prod impl + self-hosted Langfuse + Profile A air-gapped E2E. Tags `v1.4.5-phase-1-5` when done.
- **Phase 2 pre-work (PP1–PP3):** real GPU capacity benchmarks + 10 deferred contract sign-offs + HITL SLA validation. Sequencing constraints, not dated.
- **S93 housekeeping:** `test_alembic_034` final hardening, `out/` log cleanup, `CLAUDE.md` counts sync. Runs before the merge/tag.

---

## Risk

- **Code risk:** zero. S92 is documentation-only.
- **Process risk:** low. The scope correction in `104_*` §7.2/§8.1 does not reverse any DONE status — it pinpoints the actually-shipped intake contract surface and files the remaining 10 contracts under Phase 2+ pre-work.
- **Rollback:** revert the commits in order S92 → S91 → S90 → S89 → S88 on the feature branch. No state outside the repo changes.

---

## Sign-off checklist (before opening PR)

- [ ] S93 committed.
- [ ] `make lint` + `make test` green on HEAD.
- [ ] `out/` backlog cleaned.
- [ ] `CLAUDE.md` key numbers match test evidence.
- [ ] `01_PLAN/ROADMAP.md` still reflects the queue after S93 (update S92 IN-PROGRESS → DONE, S93 QUEUED → DONE).
- [ ] Tag `v1.4.4` created and pushed.

---

*Generated 2026-04-27, S92 / v1.4.4.5. Final PR body will be regenerated at S93 session-close with S93 commits appended.*
