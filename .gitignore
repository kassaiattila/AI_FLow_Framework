*.py[cod]
assets/external/
.web
.states
*.db
# === Secrets - SOHA ne commitoljuk ===
.env
.env.*
!.env.example
!.env.langfuse.example
**/credentials.json
**/secrets.yaml
jwt_private.pem
jwt_public.pem
*.pem

# Tenant-specific policy files (generated per-tenant, not shipped in repo)
config/policies/

# === Python ===
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# === IDE ===
# .vscode/ is committed (shared team settings)
.idea/
*.swp
*.swo
*~

# === Testing artifacts ===
tests/artifacts/
!tests/artifacts/.gitkeep
skills/*/tests/artifacts/
!skills/*/tests/artifacts/.gitkeep
htmlcov/
.coverage
coverage.xml
.pytest_cache/
.promptfoo/

# === Playwright ===
tests/ui/test-results/

# === Build ===
node_modules/
.next/

# === Data ===
data/
outputs/
test_runs/
*.sqlite

# === OS ===
.DS_Store
Thumbs.db
Desktop.ini

# === ML models & training data (large binary files) ===
*.joblib
skills/*/training_data/collected/
skills/*/models/*.csv
.cache/
var/

# === Test data (generated, not source) ===
test_output/
test_emails/
test_rag_docs/

# === E2E / audit screenshots (regenerated on test runs) ===
e2e-audit/
e2e-*.png
audit-*.png
test-*.png
verify-*.png
verification-*.png
dashboard-*.png
invoice-*.png
process-docs-*.png
rag-chat.png
f2-*.png

# === Session prompts (NEXT.md is ephemeral, archives committed) ===
session_prompts/NEXT.md

# === Auto-sprint runtime state (durable iteration state + notification log) ===
session_prompts/.auto_sprint_state.json
session_prompts/.notifications.log
session_prompts/.notifications_body.md

# === Temp output directories ===
output/
tmp_*/
fastapi_log.txt
test_audio.wav

# === Session-run debug output (out/ scratch pad) ===
# Committed artifacts live alongside — gitignore the transient noise only.
out/api_log*.txt
out/cli_test.*
out/*.log
out/token.*
out/spec_eval.json
out/diagram_eval.json
out/hardening_*.md
out/phase_1b_merge_gate_*.log
out/s89_journey_raw.log
out/s89_vite_dev.log
out/s90_journey_rerun.log
out/mypy_*.log

# === MCP / Figma tool configs (local) ===
.playwright-mcp/
claude-talk-to-figma-mcp/
aiflow-admin/figma-sync/reference/

# === Legacy / deprecated (archived in git history) ===
deprecated/
rxconfig.py

# === Claude Code — 5 retegu architektura ===
# COMMITTED (csapat szintu): settings.json, settings.local.json (team permissions),
#                            commands/, skills/, agents/
# NOT COMMITTED (szemelyes): CLAUDE.local.md, runtime locks
CLAUDE.local.md
.claude/scheduled_tasks.lock

# === Reference projects (separate repos, kept alongside) ===
DOHA/

# Playwright MCP + debug artifacts
*.png
dialog-snap.md
cancel-snap.md
.playwright-mcp/
