version: 1
suites:
  core-unit:
    description: "Core kernel unit tests"
    path: "tests/unit/core/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: critical
    max_duration_seconds: 30
    coverage_target: 90.0
    requires_services: []
    components:
      - core.config
      - core.types
      - core.errors
      - core.context
      - core.events
      - core.registry
      - core.di

  state-unit:
    description: "State layer unit tests (ORM models, repository)"
    path: "tests/unit/state/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: high
    max_duration_seconds: 30
    coverage_target: 85.0
    requires_services: []
    components:
      - state.models
      - state.repository

  models-unit:
    description: "Model abstraction layer unit tests (metadata, protocols, registry, client, router, cost)"
    path: "tests/unit/models/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: critical
    max_duration_seconds: 30
    coverage_target: 90.0
    requires_services: []
    components:
      - models.metadata
      - models.protocols
      - models.protocols.classification
      - models.protocols.extraction
      - models.registry
      - models.client
      - models.router
      - models.cost

  vectorstore-unit:
    description: "Vector store unit tests (base models, hybrid search, embedder)"
    path: "tests/unit/vectorstore/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: high
    max_duration_seconds: 30
    coverage_target: 85.0
    requires_services: []
    components:
      - vectorstore.base
      - vectorstore.search
      - vectorstore.embedder

  engine-unit:
    description: "Workflow engine unit tests"
    path: "tests/unit/engine/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: critical
    max_duration_seconds: 60
    coverage_target: 90.0
    requires_services: []
    components:
      - engine.step
      - engine.dag
      - engine.conditions
      - engine.policies
      - engine.workflow
      - engine.runner
      - engine.checkpoint
      - engine.serialization

  agents-unit:
    description: "Agent system unit tests (orchestrator, specialists, quality gates, human loop, reflection)"
    path: "tests/unit/agents/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: critical
    max_duration_seconds: 30
    coverage_target: 90.0
    requires_services: []
    components:
      - agents.messages
      - agents.specialist
      - agents.orchestrator
      - agents.quality_gate
      - agents.human_loop
      - agents.reflection

  prompts-unit:
    description: "Prompt platform unit tests (schema, manager, sync, A/B testing)"
    path: "tests/unit/prompts/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: high
    max_duration_seconds: 30
    coverage_target: 90.0
    requires_services: []
    components:
      - prompts.schema
      - prompts.manager
      - prompts.sync
      - prompts.ab_testing

  documents-unit:
    description: "Document management unit tests (registry, versioning, freshness)"
    path: "tests/unit/documents/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: high
    max_duration_seconds: 30
    coverage_target: 85.0
    requires_services: []
    components:
      - documents.registry
      - documents.versioning
      - documents.freshness

  ingestion-unit:
    description: "Ingestion pipeline unit tests (parsers, chunkers, pipeline)"
    path: "tests/unit/ingestion/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: high
    max_duration_seconds: 30
    coverage_target: 85.0
    requires_services: []
    components:
      - ingestion.chunkers
      - ingestion.pipeline

  skills-unit:
    description: "Skill system unit tests (manifest, loader, registry)"
    path: "tests/unit/skills/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: critical
    max_duration_seconds: 30
    coverage_target: 90.0
    requires_services: []
    components:
      - skills.manifest
      - skills.loader
      - skills.registry

  evaluation-unit:
    description: "Evaluation framework unit tests (framework, scorers, datasets)"
    path: "tests/unit/evaluation/"
    suite_type: unit
    run_on: [commit, pr, merge]
    priority: high
    max_duration_seconds: 30
    coverage_target: 90.0
    requires_services: []
    components:
      - evaluation.framework
      - evaluation.scorers
      - evaluation.datasets
